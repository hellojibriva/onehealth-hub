# processed/

Derived/computed output built from `raw/` and `source-derived/` inputs via
documented transformation logic. Every dataset in this category must carry a
`LINEAGE.md` alongside it, listing its exact inputs and the transformation
or matching logic used to produce it.

No datasets have been added yet.
