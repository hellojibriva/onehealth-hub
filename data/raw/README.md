# raw/

Source data as received, kept in its original form. Files here are never
edited in place — a correction or update from the source produces a new
version, not an overwrite. Missing values remain missing (never converted to
zero or otherwise inferred).

No datasets have been added yet.
