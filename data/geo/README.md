# geo/

**Status: placeholder — no boundary file added yet.**

Will hold verified geographic boundary assets, starting with a Nigeria
state-level boundary GeoJSON, once approved. Per project instructions:

- Only verified boundary data may be used — nothing fabricated.
- This is kept separate from tabular evidence data (`raw/`,
  `source-derived/`, `processed/`).
- Whether this file is later copied into `public/` (for direct client-side
  Leaflet fetch) or served through an API route is a decision for the GIS
  implementation step — not this scaffolding step. Per current instructions,
  `public/` is not being modified at this stage and no GeoJSON boundary is
  being added yet.
